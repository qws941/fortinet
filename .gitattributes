# Git LFS 설정
*.tar filter=lfs diff=lfs merge=lfs -text
*.tar.gz filter=lfs diff=lfs merge=lfs -text

# Python 패키지 파일들
offline-packages/* filter=lfs diff=lfs merge=lfs -text

# 폐쇄망 배포 파일
offline-deployment/*.tar.gz filter=lfs diff=lfs merge=lfs -text
